FROM python:3.10-slim

WORKDIR /app

# 安装依赖
COPY webui/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY webui/ .

# 暴露端口
EXPOSE 5000

# 启动应用
CMD ["python", "app.py"]
